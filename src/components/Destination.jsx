import React from 'react';

const Destination = (props) => {
    return (
        <>
            <input type="text" />
            <input type="text" />
            <button>Submit</button>
        </>
    )
}

export default Destination